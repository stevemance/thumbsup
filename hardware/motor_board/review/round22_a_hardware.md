# Round 22 — A: hardware review (rev L, after round 21)

Scope: the round-21 text (§8 boot order and DRV8316 row, §8.1 "drives resume", the split drive
row 6, the weapon positive-torque / negative-power coast, the INA239-failure confirmation, §9
step 6).  I checked it against the datasheets, RM0440, the netlist and the sims.  The package was
copied to /tmp/r22a (without datasheets/ and review/).  `motor_board.py` regenerates
`nets.md`/`netlist.csv`/`bom.csv` byte-identical ("checks: OK", 237 refs, 160 nets, 67 BOM
lines).  `sim_arm.py` and `sim_hotplug.py` reproduce `arm.out` and `hotplug.out` exactly.

References: [D] DESIGN.md line; [DRV] SLVSH07 (DRV8316C.pdf); [MT] MT6701CT-STD.pdf;
[RM] RM0440 rev 7; [C] design/calcs.md.

Motor numbers used below come from [C] §10: KV 3500, 6 pole pairs, λ = 0.263 mWb
(= 13.3 V line-line peak at 46.5 k rpm).  At top speed ω_e = 29.2 krad/s, E_phase = 7.7 V, and
1 µs of timing error equals 1.67° electrical.  Per-phase loop R = ½ R_LL (0.1–0.15 Ω) plus one
FET (RDS(on) HS+LS total 95–185 mΩ [DRV p.11]), about 0.15–0.24 Ω.  L_phase is 5–20 µH (not
published).

**Counts: 0 BLOCKER, 1 MAJOR, 3 MINOR, 3 NOTE.**

| ID | Class | Where | Finding | Evidence | Fix |
|---|---|---|---|---|---|
| R22A-01 | **MAJOR** | [D] l.675 "drives resume" step (2): "encoder, or the observer's catch of a turning rotor; with no valid angle and the motor turning, wait until it is slow"; [D] l.614 start angle: "unless the encoder or the observer shows the rotor moving (then run sensorless until it has stopped)" | **The drives cannot catch a turning rotor without the encoder, and cannot tell whether it is turning.  Both resume paths that assume otherwise do not exist on this hardware.**<br><br>**Why no catch is possible:**<br>• The drives have no phase-voltage sensing.  L_A/L_B/L_C go only to U3 OUTx and J_Lx; R_x only to U4 and J_Rx.  The R22–R27 dividers exist on the weapon only.<br>• While the chip is coasted (CTRL4 DRV_OFF or the pin), the phase current is exactly zero.  A flux observer then has no input: it would integrate its own commanded voltage, not the floating terminal voltage.<br>• INLx is tied to +3V3, so 3x mode has no per-phase Hi-Z [DRV Table 8-4].  A released chip always drives each phase H or L.  Any current-probing catch (zero-vector pulses) therefore needs a released chip, and between pulses it is braking.<br>• In the no-angle case, the firmware has no speed information while the drive is coasted.  "The motor turning" and "until it is slow" cannot be evaluated.<br><br>**Who is affected:**<br>• Every resume in the documented fallback ("if the magnet does not work out: sensorless FOC", [D] §3.3).<br>• Drive row 7 (both drives sensorless until an operator clear).<br>• A drive switched to sensorless after a sensor fault.<br>• Any MCU reset while the robot moves (angle invalid).<br><br>**Consequence:** a resume while the robot coasts or is pushed at > ~0.5–1 m/s applies a brake or a wrong vector: 10–39 A (table in N1) against OCP 10–22 A.  Each OCP is counted.  More than ~3 in a second becomes drive row 2, whose action calls "drives resume" again.  More than ~3 row-2 events in a second **latches the drive, persisted across resets**.  Nothing limits the retry rate, so a link blip or a bounce while being shoved can immobilise a sensorless robot.  The chip itself survives (OCP latched, Hi-Z within ~1 µs). | nets.md L_A/L_B/L_C, R_A/R_B/R_C; [D] §3.2 phase-sense row (R22–R27 → PA4/PA5/PA2, weapon only); [DRV] Table 8-4 p.22 (3x truth table), §8.4.2 p.53; [D] l.677 (row 2 latch), l.141–145 | Rewrite the angle sources in step (2) and in the start-angle rule:<br>(a) **Encoder present, angle invalid** (MCU reset, sensor re-power): use **Z**.  The first Z arrives within one motor revolution (1.3 ms at 46.5 k rpm, 6 ms at 10 k rpm) and gives the absolute angle from the stored Z offset (§9 step 5).  Also require a Z pulse since the last supply event or sensor dropout before a zero-current Release above a low speed.  This covers an MT6701 brown-out between its 3.0 V minimum and the 2.8 V BOR that leaves the count wrong but "valid".<br>(b) **No usable encoder**: delete "observer's catch".  Resume only after a fixed coast time, measured in §9 from top speed, or on a speed hint from the compute board.  Rate-limit resume retries after a resume OCP (e.g. ≥ ~300 ms apart), so they can never reach the row 2 latch; report instead.<br>(c) Optional, bench-only: when **both** drives resume together, short low-side zero-vector probes gated by the shared DRV_OFF pin (a few µs, CSAs see low-side current, tSET ≤ 1 µs [DRV p.13]) could give a catch.  The DRVOFF pin timing is unspecified, so do not rely on this without measurement. |
| R22A-02 | MINOR | [D] l.676 step (3) "it leaves Hi-Z with its outputs already at the back-EMF, so no current step"; §9 step 6 top-speed resume test | **"No current step" holds only if the zero-current voltage matches the back-EMF to within ~10° and ~10–20 %.**  While the chip is Hi-Z the CSAs read exactly zero, so the current loop provides no correction.  What gets applied is pure feed-forward, and every latency that closed-loop FOC normally hides appears as a step at the Release.<br><br>Step current after Release at 46.5 k rpm, as angle error → steady / peak with transient (L 5 µH, R 0.15 Ω; 20 µH in brackets):<br>• 5° → 3.2 / 3.4 A (1.1 / 1.6 A)<br>• 10° → 6.4 / 6.8 A (2.2 / 3.3 A)<br>• 15° → 9.6 / 10.2 A (3.3 / 4.9 A)<br>• 20° → 12.7 / 13.6 A<br>• 50° → 31 / 33 A<br><br>OCP_LVL 0 trips at 10 A minimum [DRV p.13].<br><br>Error sources at top speed:<br>• PWM compute and update latency, ~1.5 T_PWM = 31 µs = **52°** if uncompensated.<br>• MT6701 system delay, 5 µs typ, no min/max [MT p.7 "TDelay"] = 8.4°.<br>• DRV8316 delay target 1.8 µs = 3°.<br>• MT6701 INL ±1.5° mechanical max [MT] = **±9° electrical**, position-dependent.  The Z/alignment offset removes only a constant.<br>• λ tolerance: ±10 % ≈ 0.77 V ≈ a 5.7° equivalent.<br><br>Even when fully compensated, INL alone can give 5–7 A with a low-L motor.  The §9 test catches a failure but gives no fallback. | [DRV] p.11–13 (RDS(on), tPD, IOCP, tOCP, CSA offset ±50 mA); [MT] p.7 electrical table (TDelay 5 µs, INL ±1.5°); [C] §10; calculation above | State in step (2) that the zero-current voltage includes explicit latency compensation (PWM delay, MT6701 5 µs, DLY_TARGET), with λ from the bring-up measurement, and PI integrators preset to the feed-forward.  Add a fallback if the §9 top-speed resume shows > ~6 A: either cap the resume speed with a valid angle (wait until below it), or raise OCP only for the resume window (OCP_LVL = 1, 15/24/30 A [DRV p.13]; firmware computes the parity).  In §9, record the peak phase current at the Release vs speed (current probe), not only "no OCP". |
| R22A-03 | MINOR | [D] l.675 step (2) "BKE = 0 on TIM8"; step (4) "then BKE = 1 again"; contradicts [D] l.606 "CLL needs BKE = 1 on every timer it should stop" | **Clearing BKE disables the whole TIM8 break, including the CLL lockup system break, for the entire resume window (up to ~10 ms).**  [RM] TIMx_BDTR BKE: "enables the complete break protection (including all sources connected to bk_acth and BKIN sources)".  The system-break interconnect table puts tim_sys_brk0 = Cortex-M4 LOCKUP (CLL).  If the core locks up inside the window, TIM8 keeps PWM at MOE = 1 with frozen CCRs until the 20 ms IWDG.  At speed a frozen vector draws the OCP current: the DRV8316 protects itself, so this is not destructive, but it is exactly what CLL was set up to prevent.  The same need is met by clearing only the pin: TIMx_AF1 **BKINE** (bit 0, reset 1) "enables the TIMx_BKIN alternate function input… ORed with the other tim_brk sources".  That is already the mechanism TIM20 uses (BKE = 1, BKINE = 0).  Setting MOE with L_nFAULT low does need one of the two: MOE "is cleared asynchronously by hardware as soon as one of the break inputs is active" ✓ | [RM] §28.6.x TIMx_BDTR bits 15/12, TIMx_AF1 bit 0, Table 256 (system break interconnect); [D] l.606 | Step (2): "TIM8_AF1.BKINE = 0 (BKE stays 1, so CLL still stops TIM8), clear BIF, set MOE".  Step (4): "BKINE = 1 again".  Keep TIM8 BDTR LOCK at 0: BKINE, BKE, OISx and OSSI cannot be written at LOCK ≥ 1 [RM]. |
| R22A-04 | MINOR | [D] l.652 weapon row 0: "a weapon with a positive torque command but negative electrical power is instead coasted at once: that is the signature of the switch opening under throttle … and the coasting-drum rules then apply" | **A contact bounce under throttle produces the same signature, often with no break event, and the rule gives no path back to spinning.**  At 20 A on ~374 µF [C] the bus falls ~53 V/ms and passes the drum's back-EMF (~0.8–0.9 × VBUS) in ~40–60 µs.  After that the drum rectifies into the bus and holds it there, so:<br>• the U2 UVLO is never reached;<br>• no BUVL is set (reset value);<br>• the comparators see nothing.<br>With no break, weapon rows 1–12 never run, and the "coasting-drum rules" contain no restart.  Row 1's "pack current returns within 100 ms → restart as supply" applies only after a BOVL.  The result is a weapon that stays coasted after a common impact bounce, with the restart left undefined.  The rule also has no threshold: near idle (~1 A), the P = 1.5(v_d·i_d + v_q·i_q) estimate sits within its own noise (CSA offset, VBUS lag). | [C] §9 bus decay row (~374 µF); [D] l.653 (row 1 "looks open" restart), l.652 | Treat it as row 1 "looks open" does: coast, then restart as a supply-class event (counted in the budget) if pack current returns within row 0's 100 ms window; otherwise row 0 holds.  Require the electrical power (or i_q sign against the command) to be below a threshold (e.g. < −~5 W or i_q < −~1 A) for ≥ a few control periods.  §9 step 6: pack-lead interruptions of 0.3–3 ms with the weapon under throttle → restart, no hold. |
| R22A-N1 | NOTE | [D] l.675 "the limit, ~0.8 m/s wheel speed by estimate" | Brake (zero-vector) current vs wheel speed, peak including the transient, R 0.15 / 0.24 Ω × L 5 / 20 µH:<br>• 0.5 m/s: 4.1–6.9 A<br>• **0.8 m/s: 6.2–10.8 A**<br>• 1.2 m/s: 8.5–15.9 A<br>• 3.7 m/s: 16–39 A<br><br>The round-21 estimate (< ~6 A at ~0.8 m/s) used the line-line R as the per-phase R.  Expect the §9 measurement to land nearer ~0.5 m/s.  §9 already measures it, so there is nothing to change beyond the "~0.8 m/s" wording. | calculation above; [DRV] p.11 RDS(on), p.13 IOCP | optional: "~0.5–0.8 m/s" |
| R22A-N2 | NOTE | [D] l.677 step (4) "if still low, run the fault recovery … once and wait again" | If the low nFAULT is a resume OCP, the chip is already released (CTRL4 0x10).  CLR_FLT re-enables it straight into the same mismatch, and it re-trips within µs.  This is harmless for the chip (OCP latched, 0.6 µs deglitch), but it wastes the retry and adds a count.  Inside a resume, re-coast first (CTRL4 0x0C90), then CLR_FLT. | [DRV] §8.3.14.6, §8.4.1.3, Table 8-21 | optional |
| R22A-N3 | NOTE | verified | **DRV8316:**<br>• CTRL4 bit 7 DRV_OFF "1h = Hi-Z FETs" exists only in the register map, with no timing or transition specification [DRV Table 8-21 p.65].  "No current step" rests on it following INHx the moment it is cleared; the charge-pump high-side drive has no bootstrap refresh issue.  The §8.1 bench item and §9 test are the right checks.<br>• With the DRVOFF pin high, OCP is inactive and nFAULT may go low [§8.4.2 p.53] ✓.<br>• 3x truth table: INL = 1, INH selects H/L [Table 8-4] ✓.<br>• IOCP 10/16/22 A (24 A setting 15/24/30 A); tOCP_DEG 0.2/0.6/1.2 µs; CSA offset ±50 mA; tSET ≤ 1 µs [p.13] ✓.<br>• 0x0603/0x0606/0x0C90/0x0D10/0x097D: addresses and even parity ✓; REG_LOCK 011b unlock / 110b lock [Table 8-18] ✓.<br>**Timers:** MOE cannot stay set while an enabled break input is active [RM BDTR MOE], so the resume must disable the L_nFAULT break before MOE ✓ (see R22A-03 for how).<br>**INA239 confirmation:** ≥ 3 consecutive bad polls with an immediate rewrite on mismatch is consistent with the hardware.  A reset INA239 loses SOVL/BOVL until the rewrite, which happens on the first mismatching poll ✓.<br>**Circuit:** U3/U4 pin numbers match [DRV Table 6-1] ✓; netlist and sims reproduce ✓. | as cited | — |

No other issues were found in the round-21 text or in the circuit it depends on: the DRV_OFF net
(R50 pull-up, DRVOFF internal pull-downs), L_nFAULT → PB7 TIM8_BKIN, R_nFAULT → PC15 EXTI, INL
tied to +3V3, and the SOx filters.
