# Round 23 — A: hardware review (rev L, after round 22)

Scope: the round-22 text (§8.1 "drives resume" steps 1–4, the Z-pulse / fixed-coast angle rules,
TIM8_AF1 BKINE, the optional OCP_LVL 24 A resume words, the weapon negative-power coast in row 0,
drive rows 6/7) and the circuit it relies on.  The package was copied to /tmp/r23a (without
datasheets/ and review/, deleted afterwards).  `motor_board.py` regenerates
`nets.md`/`netlist.csv`/`bom.csv` byte-identical ("checks: OK", 237 refs, 160 nets, 67 BOM lines);
`sim_arm.py` and `sim_hotplug.py` reproduce `arm.out` and `hotplug.out` exactly.

References: [D] DESIGN.md line; [DRV] SLVSH07 (DRV8316C.pdf), printed page; [MT] MT6701CT-STD.pdf
page; [RM] RM0440; [C] design/calcs.md; [N] design/nets.md.

**Counts: 0 BLOCKER, 1 MAJOR, 1 MINOR, 4 NOTE.**

| ID | Class | Where | Finding | Evidence | Fix |
|---|---|---|---|---|---|
| R23A-01 | **MAJOR** | [D] l.652 row 0: "a weapon motoring command … with electrical power below ~−20 W for ≥ ~1 ms **while the INA239 reads ≈ 0 A (\|I\| < ~50 mA) and firmware's signed estimate does not** is instead coasted at once"; same row: row 0 "suspended while any actively driven motor's electrical power is negative … or while firmware's own pack-current estimate (idle draw plus the actively driven channels only) is within ~50 mA of zero"; §7.5 "Firmware detects it … and coasts the weapon and stops the drives" | **The round-22 gate cannot be met by a real open switch, so an open switch is not detected while the weapon is FOC-driven.**<br><br>**1. With the switch open, the net estimate is ≈ 0 by energy conservation.**<br>• Once the switch is open, RS4 carries only U13 + the R13/R14 divider + C14 on BAT_IN (~0.2 mA) [N BAT_IN].  So the INA239 reads ≈ 0 ✓.<br>• Nothing else supplies the bus.  An FOC-driven drum therefore settles where its regen exactly feeds the board: P_weapon = −(P_idle + P_drives), with only C1's C·dV/dt left over.<br>• The estimate (idle + drives + weapon) is therefore ≈ 0 too, apart from C1's current and estimation error.<br>• "INA239 ≈ 0 **and the estimate is not**" never holds for ≥ 1 ms.  The only exception is the C1 recharge transient (374 µF × a few V at ~10 A ≈ 0.1 ms).<br>• Case 1 (switch open under throttle) and case 2 (closed switch, regen balancing the load) look the same to this gate.  It rejects both.<br><br>**2. The −20 W threshold is also unreachable with the drives idle.**  The drum then supplies only the idle draw, 100–120 mA × ~15 V ≈ 1.6–1.9 W [C l.121, §7.17].  P_weapon ≈ −2 W, which is above −20 W.<br><br>**3. Row 0 stays suspended at the same time, by its own two clauses:**<br>• the weapon (an actively driven motor) has negative power;<br>• the net estimate is within 50 mA of zero.<br>The "coasting motor never suspends it" exemption does not apply, because the drum is not coasted.<br><br>**This does not depend on the throttle.**  Zero-torque FOC also regenerates once the bus has sagged to where the modulator saturates at E.<br><br>**Consequence:** after the switch opens with the weapon under FOC:<br>• the board stays up on drum energy;<br>• the drives remain commandable (their load raises P_weapon below −20 W, but the estimate is still ≈ 0);<br>• nothing coasts.<br>This lasts until the drum slows to where the buck drops out.  The bus falls below ~9.2 V at E_LL ≈ 8 V, ~14 k rpm.<br><br>Using the package's spin-up windage model (T = 2e-9·ω², J from 64 J at 25.6 k rpm), that takes ≈ 2.6 s with the drives idle and ~1.5 s with them driving.  The main power switch does not stop the robot for 1.5–2.6 s.<br><br>The §9 step 6 test "with the weapon throttle held → switch-open hold, drives stopped within ~0.1 s" would fail, and the text gives no rule that passes it. | [N] BAT_IN (C12, C14, J_BAT+, Q7 D, R13, U13 VS), VBAT_SW (Q8 D, R2, RS4); [C] l.121 idle 100–120 mA; round22_d R22D-01 model (25.6 k rpm, 2.7 A, 15.24 V, i = 2e-9·ω²/Kt → ~38 W windage at top speed); decay t = (J/c)(1/ω₁ − 1/ω₀) = 8925 × (1/1508 − 1/2678) ≈ 2.6 s | Use the one measurement that differs between open and closed, the INA239 alone, and drop the estimate gate and the −20 W threshold.<br><br>**New rule:** INA239 \|I\| < ~50 mA on ≥ 3 consecutive conversions (~1 ms at 150 µs/150 µs) while the weapon is FOC-driven with electrical power ≤ ~0 → coast the weapon (counted, reported).<br><br>**What follows:**<br>• The coasted weapon no longer counts, so the net estimate becomes idle + drives ≥ ~0.1 A.  Row 0's suspension then lapses, and row 0 confirms within its 100 ms.<br>• If the switch is closed, the INA239 shows the idle + drive load within 1–2 conversions after the coast.  Restart via catch-spin at once (already specified; it can happen before the 100 ms).<br>• A closed-switch false coast needs the drum's regen to cancel the whole board load to within ±50 mA for ~1 ms.  That is a rare coincidence, not the 0.28 V-dip case R22D-01 was about: that case gives a non-zero pack current.<br><br>Also fix §7.5 if the rule changes. |
| R23A-02 | MINOR | [D] l.675 "release at CTRL4 OCP_LVL = 24 A for the resume window only (coast 0x0D94 → release 0x0C14, then back to 0x0D10)"; l.612 register check "CTRL4 0x10 released / 0x90 coasted" | **The ~100 Hz register check does not know the 24 A values.**  Inside the resume window CTRL4 reads 0x94 or 0x14.  A poll that lands there sees a register mismatch, which is drive row 2: per-drive coast, recovery, rewrite, then "drives resume" again.<br><br>This aborts the firmware's own resume.  More than ~3 in a second latches the drive, persisted.  Whether this happens is a matter of timing: the window is a few ms against a 10 ms poll, and longer with the (4) repeat.  The only existing exception covers IC_STAT FAULT, not CTRL4. | [DRV] Table 8-21 p.65 (bit 2 OCP_LVL: 0 = 16 A, 1 = 24 A); [D] l.612, l.682 | Register check: "CTRL4 also 0x94 / 0x14 while the 24 A resume option is in its window", or pause the CTRL4 comparison for a chip in a drives-resume step.  Also make sure the "back to 0x0D10" happens before the check resumes. |
| R23A-N1 | NOTE | [D] l.675 "(TIM8_AF1 BKINE = 0 … BKE stays 1 so the CLL lockup break still acts)" | **Correct.**<br>• PB7 = TIM8_BKIN [ref/ST XML] ✓.<br>• TIMx_AF1.BKINE gates only the BKIN pin (reset 1); the system break (CLL/lockup) is gated by BDTR.BKE ✓ [RM TIMx_AF1, TIMx_BDTR].<br>• With BKINE = 0, BIF can be cleared and MOE set while L_nFAULT is low ✓.<br>• The L_nFAULT EXTI (line 7) still records, independent of the pin's AF mode ✓.<br><br>**One condition goes unstated:** BKINE (like BKE/OSSI/OISx) cannot be written once BDTR.LOCK ≥ 1.  So state "keep TIM8 BDTR LOCK = 0".  LOCK is not mentioned anywhere today, so its reset value of 0 already holds. | [RM] TIMx_AF1 bit 0 note, BDTR LOCK; ref/STM32G474RxTx_pins.xml PB7 | optional one-liner |
| R23A-N2 | NOTE | [D] l.677 step (4) "if still low, re-coast … and repeat from (2)"; l.675 "only for steps 2–4, a few ms"; (2) "OCP … retried every ~0.5 s" | **In the no-encoder retry loop, "repeat from (2)" keeps BKINE = 0 and MOE = 1 through the ~0.5 s retry wait.**  This is not "a few ms".<br><br>During that time a chip that resets silently comes back with CTRL4 = 0x10: released, and in 6x mode.  The break that would idle INH high (6x → Hi-Z) is disabled.  The result is at most a latched OCP (the chip protects itself), so it is harmless. | [DRV] Table 8-21 reset 10h; [D] l.682 (OISx/OSSI rationale) | Between retries: MOE = 0 and BKINE = 1; step (2) sets them again |
| R23A-N3 | NOTE | [D] l.686 row 6 (a) "the other moved in step with both alignment steps (~171 counts each, matching sign), on two consecutive alignments" | **~171 counts is right for the second step, not always for the first.**<br>• The second step is 90° electrical = 15° mechanical (6 pole pairs) = 4096 × 15/360 = 170.7 counts ✓.<br>• The first step (rest → +90°) moves the rotor by an unknown 0–±180° el (0–341 counts) on a first alignment.<br>• On a repeat it is ~171, because the rotor rests at 0° from the previous alignment.<br>So a true crossed case reaches row 7 only after one (c) repeat: three alignments instead of two.  It still works within the ~5 repeats. | [D] l.614 (two-step alignment), [C] §10 (6 pole pairs), MT6701 1024 PPR | optional: "second step ~171 counts; first step ~171 counts on a repeat" |
| R23A-N4 | NOTE | verified | **DRV8316 SPI words:**<br>• CTRL4 layout: bit 7 DRV_OFF, 6 OCP_CBC, 5:4 OCP_DEG (reset 1 = 0.6 µs), 3 OCP_RETRY, 2 OCP_LVL, 1:0 OCP_MODE [DRV Table 8-21 p.65] ✓.<br>• Frame: W B15, address B14:9, parity B8 (even number of 1s in the whole word), data B7:0 [DRV §8.5.1.1 p.64].<br>• Recomputed: 0x0603, 0x0606, 0x1019, 0x0A4E, 0x0C90, 0x0F00, 0x1818, 0x087C, 0x097D, 0x0D10, **0x0D94** (addr 6, 0x94 = DRV_OFF + DEG 0.6 µs + LVL 24 A, latched, P = 1), **0x0C14** (0x14, P = 0) — all even parity, correct address and fields ✓.<br>• IOCP 10/16/22 A (LVL 0), 15/24/30 A (LVL 1) [DRV p.13] ✓.<br>• DRVOFF pin: OCP inactive, nFAULT may go low [DRV §8.4.2 p.53] ✓.<br><br>**MT6701:**<br>• TDelay 5 µs typ (no min/max), INL ±1.5° max = ±9° electrical, TPwrUp ≤ 32 ms [MT p.7] ✓.<br>• Z width register 1–16 LSB / 180° [MT p.16] ✓.<br>• Z at the EEPROM ZERO position, so a stored Z electrical offset is absolute ✓.<br>• The "±9° … compensated" wording in (2) can only mean a per-unit angle calibration (e.g. constant-speed linearity fit in §9 step 5), not latency compensation.  The resume speed cap covers it if that is not done.<br><br>**Resume preset:** λ = 60/(√3·2π·KV·pp) = 0.263 mWb for 3500 KV, 6 pp (matches [C] §10).  V_q = ω·λ = 7.7 V phase peak at 46.5 k rpm; 15° → 2E·sin 7.5°/\|Z\| ≈ 9.6 A at L = 5 µH ✓ ("~10 A").<br><br>**Switch-open current:** the INA239 sees only BAT_IN's ~0.2 mA (U13 45 µA, R13+R14 115 k, C14), so "\|I\| < 50 mA" is the right open signature (40 LSB at 1.25 mA/LSB) ✓.  INA239 words SOVL 0x76C0, BOVL 0x17C0, SHUNT_CAL 0x1000, ADC_CONFIG 0xB480 re-checked ✓.<br><br>**Circuit:** DRV_OFF net (R50 pull-up, PC14, U3/U4 pin 21), L_nFAULT → PB7, R_nFAULT → PC15, INL tied to +3V3 [N] ✓; netlist and sims reproduce ✓. | as cited | — |

No other hardware issues were found in the round-22 text or the circuit it depends on.
