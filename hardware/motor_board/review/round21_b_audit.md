# Round 21 — B: consistency audit (rev L, after round 20)

Scope: the whole package for consistency, with a focus on the round-20 edits to DESIGN.md §8, §8.1 and §9.
Board area and fit, and prose style, are out of scope.  Line numbers refer to the working tree as of 2026-09-24.

## Reproduction

- The package was copied to `/tmp/r21b/`.  `design/motor_board.py` prints `237 refs (205 placed components), 160 nets, 67 BOM lines` / `checks: OK`.  `design/calcs.py` runs clean.
- `netlist.csv`, `nets.md`, `bom.csv`, `mcu_pinmap.md` and `calcs.md` regenerate byte-identical to the tree.
- bom.csv has 67 lines and ΣQty = 199.  Qty matches the designator count on every line.  The 6 DNP parts (C110–C112, C114–C116) are left out of the CSV, and 199 + 6 = 205 placed.  README.md:7, BOM.md:4–5 and DESIGN.md:37 agree.
- Every LCSC code in BOM.md, DESIGN.md and datasheets/README.md is either on a bom.csv line or a named alternate (RBT6/RCT6, the DRV8323 alt listing, INA229, the vertical XH, LVC3G17 DCT, 74LVC08APW).
- U1 pins against AFs.  All 64 pins were spot-checked:
  - TIM1 CH1–3 on PC0/PC1/PA10, CH1N–3N on PA7/PB14/PB15, BKIN on PC13.
  - TIM8 CH1–3 on PB6/PC7/PB9, BKIN on PB7.
  - TIM20 CH1–3 on PB2/PC2/PC8.
  - TIM3 CH1–3 on PC6/PB5/PB0; TIM2 CH1–3 on PA15/PB3/PB10.
  - SPI3 on PC10–12; USART1 on PC4/PC5.
  - COMP3/1/6 inputs on PA0/PA1/PB11; OPAMP5 VINP2 on PC3.
- DRV8316 SPI words.  All 11 words in DESIGN (0x0603, 0x1019, 0x0A4E, 0x0C90, 0x0F00, 0x1818, 0x087C, 0x097D, 0x0606, 0x0D10, and TI's 0x1915) decode to the stated register (A[14:9]).  Each has an even number of 1s including B8.  The data bytes match the stated fields and the read-back expectations: CTRL2 0x7C, CTRL3 0x4E, CTRL4 0x10/0x90, CTRL6 0x19, CTRL10 0x18.
- INA239 register values:
  - ADC_CONFIG 0xB480 = MODE Bh, VBUSCT 2h, VSHCT 2h, VTCT 0, AVG 0.
  - SHUNT_CAL 0x1000 → 1.25 mA/LSB at 1 mΩ with ADCRANGE = 1.
  - SOVL 0x76C0 = 30 400 × 1.25 µV = 38.0 mV (38 A).
  - BOVL 0x17C0 = 6080 × 3.125 mV = 19.0 V.
  - DEVICE_ID 2391h.
- Cross-references to §/row/step all resolve: §7.2/7.3/7.15/7.16/7.17/7.21, §6.13, §9 steps 0–6, weapon rows 0–12, drive rows 1–7/6a.  Revision and round labels (rev L, rounds 11–20, "twenty") are consistent.

## Findings

| ID | Class | Where | Finding | Evidence | Fix |
|---|---|---|---|---|---|
| B21-01 | **MAJOR** | DESIGN.md:682 (drive row 6), :684 (row 7), :614 (timer inputs), :766–768 (§9 step 6) | **Drive row 6 is first-match ahead of row 7 and swallows its whole signature, so the left/right-crossed report can never fire.  Row 6 also contradicts the timer row's "too little motion" handling.**<br>• With crossed J2/J3 sensor cables or crossed motor bundles, the aligning drive's own encoder reads ~0 counts and the other encoder reads ~171.<br>• Row 6 matches this case twice: "the aligning drive moved outside 0.5–2× the expected ~171 counts" (0 < 0.5×), and "either encoder was not still around a step".<br>• Row 6 therefore wins ("repeat the alignment; not counted") and the alignment repeats forever.  Row 7 ("the other encoder moved while the aligning drive's own encoder stayed still") is unreachable.<br>• §9 step 6 expects "alignment reports 'left/right crossed'" for both swaps, so it fails as written.<br>• The same clause (< 0.5×) also covers the timer row's "too little motion → retry at a higher current, then run sensorless and report" (:614).  Under first-match that case now loops at the same current instead.  A gearbox that holds the rotor (the case the two-step alignment exists for) then leaves the drive unusable after an MCU reset, when it should fall back to sensorless.<br>• Row 6 is not counted, so nothing bounds the loop | :682 "moved outside 0.5–2× … or either encoder was not still around a step, or both encoders moved"; :684; :614 "too little motion → retry at a higher current, then run sensorless and report"; :767–768 | Order the checks by signature.  (1) Own encoder still (< ~10 %) and the other moved ≈ expected → row 7.  (2) Both moved → row 6 (repeat).  (3) Own moved < 0.5× with the other still → the timer-row path (higher current, then sensorless and report).  (4) Own moved > 2×, or not settled around a step → row 6.  Give row 6 a cap, e.g. more than ~5 repeats → sensorless and report |
| B21-02 | **MAJOR** | DESIGN.md:652 (row 0 suspension and estimate), :694–696, :521–522 (§7.5), :736–737 (§9 step 3) | **If the switch opens while the weapon is under throttle (not coasting), row 0 is suspended for as long as the drives draw, and the cross-check and plausibility gate are both off.  The drives stay commandable on drum energy after the switch is opened.**  This is the round-20 case (B20-01/R20A-02) with the drum *driven* instead of coasting.<br>• When the switch opens, C1 (374 µF) under several amps falls below the drum's BEMF (14.2 V at 25.6 k rpm) within ~0.1–0.3 ms.<br>• From then on the weapon bridge can no longer drive current in.  Its phase current reverses through the body diodes (rectification), so the actively driven weapon's electrical power is **negative**.<br>• Row 0 is "suspended while any **actively driven** motor's electrical power is negative", and each suspension restarts the 100 ms mean.  The second suspension clause fails too: by energy balance, the estimate (idle + the driven channels, now including a generating weapon) sits near 0.<br>• The cross-check runs "only while the drum is stopped or motoring", and a generating drum is neither.  The ≥ 1 A plausibility clause needs the weapon motoring.  The "bus clearly above BEMF" clause is false.<br>• Nothing fires.  The drives run until the drum decays to the ~9.2 V logic cutoff (~1–3 s at 10–40 W per R20A-02), or until a stall cut-out if one happens to trigger.  This contradicts §7.5 ("Firmware detects it … and stops the drives") and row 0's own "whether or not the drum is driven".<br>• The case is realistic: the switch knocked open by a hit mid-match, or switched off without disarming first.  With the pack connected, the weapon's duty cap keeps its BEMF below the bus, so an actively driven weapon at positive torque with negative power is itself evidence that the switch is open | :652 "suspended while any actively driven motor's electrical power is negative … or while firmware's own pack-current estimate (idle draw plus the actively driven channels only) is within ~50 mA of zero"; :652 "cross-check (only while the drum is stopped or motoring…)"; :522; round19_d R19D-02 (duty cap: BEMF ≤ ~0.9 × bus); round20_a R20A-02 (drum energy, 1–3 s) | Add to row 0 (or to §8 weapon safety): an actively driven weapon whose electrical power goes negative while its torque command is positive (bus below its BEMF) → coast the weapon immediately.  The existing coasting-drum logic then applies, and row 0 decides within 100 ms.  Limit the negative-power suspension to *commanded* regen (braking torque command) and back-driven drives.  In §9 step 3, run the "while driving" case once with the drum coasting and once with it under throttle |
| B21-03 | MINOR | DESIGN.md:636–638 (latch word) vs :695–696 (INA239-failure exit) | **The latch-word contents list is stale after B20-05.**  The INA239-failure exit says "the weapon stays off until the operator clears it (stored in the latch word)".  The latch-word paragraph lists weapon/left/right latches, reason, "supply unstable", the row 7 hold, the row 1 count and the supply-budget history, but not this hold.  An MCU reset during an INA239 failure then drops the hold, although it re-enters at once if the part is still failing | :636–638; :695–696; CHANGES round 20 B20-05 | Add "the INA239-failure hold" to the latch-word list (or say it is the weapon latch with reason "INA239") |
| B21-04 | MINOR | DESIGN.md:612 (100 Hz read-back: "IC_STAT FAULT (expected 0 on a released chip)") vs :673 (exemption), :605 | **The new IC_STAT FAULT expectation conflicts with the DRV_OFF-pin exemption.**<br>• SLVSH07 §8.4.2 (DRV8316C.pdf, "8.4.2 DRVOFF functionality", Note): the DRVOFF pin "can trigger fault condition resulting in nFAULT getting pulled low".<br>• A *released* chip (CTRL4 0x10) whose pin is high reads FAULT = 1.  The pin is high during the link-loss coast, a supply hold, the switch-open hold and the SPI3-fault retry.<br>• The poll says a mismatch goes to §8.1 drive events.  Both chips mismatch at once, which is drive row 1, "Supply", counted in the supply budget.  Repeated 100 Hz polls during a normal link-loss coast would then build up to "supply unstable" (> ~20 per minute, latched).<br>• The exemption at :673 covers "an nFAULT while the DRV_OFF pin is high".  It does not clearly cover the register-check path | :612; :673; DRV8316C.pdf §8.4.2 Note | ":612: IC_STAT FAULT expected 0 on a released chip with the DRV_OFF pin low, outside a drives-resume step; not compared otherwise" |
| B21-05 | MINOR | DESIGN.md:733–737 (§9 step 3) vs :744 (step 4), :754–755 (step 5), :756 (step 6) | **§9 step 3 now depends on later steps.**<br>• "Open the switch with the drum spinning" and (new in round 20) "repeat while driving both wheels → switch-open hold" come in step 3.<br>• Row 0 depends on the idle current, which is measured only in step 4.  The pack arrives in step 4, the drive motors first run (and FOC is first closed) in step 5, and the weapon is first spun and the drum fitted in step 6.<br>• In step order, these tests use motors and a drum that have not been brought up yet | :736–737; :744 "measure the minimum idle current (the idle-draw term of the §8.1 row 0 estimate)"; :754–755; :756–757 | Move the two switch-open tests (drum spinning; while driving) to the end of step 6, next to "open the power switch while braking the drum" (:764–765) |

## Notes (no action required)

- **N1 — row 9 with a failed INA239 (:661).** "Report the reason as 'INA239', the weapon is already off" leaves open whether the Action column's "gate-drive fault → latch the weapon (clearing it also needs a W_EN low pulse)" still applies.  Say "classify as the INA239 hold, not a gate-drive latch" or the reverse.
- **N2 — drives resume, silent reset without a supply event (:673).** "Counted once with the supply event" does not cover the resume paths that have no supply event (link recovery, switch-open clear, SPI3 retry).  Say what it counts as there, presumably drive row 2 for one chip and row 1 for both.
- **N3 — row 1 timing vs the classification instant (:631–632, :653).** The SPI owner classifies after the *first* post-event conversion (~0.45 ms), but row 1 needs the first *two* (~0.6–0.9 ms).  The implied wait is harmless, but it could be stated in row 1.

## Verified clean (round-20 edit areas)

- §8 boot order (:593–600): IWDG → dead-battery → clocks → heartbeat → GPIO → SPI3 → configure → W_EN → offsets → strap test → Release → drives resume.  Consistent with the watchdog row (:606: TIM1 break at boot, TIM8/TIM20 only via resume), the timers row (:613, "DRV_OFF low only when commanded"), the DRV8316 row (:612, Release only for an unlatched, commanded drive) and drive row 2 (latched drives are not released at boot).
- INA239 row (:620) and SPI health (:692): values correct (see Reproduction).  CNVR = 0 does not stop CNVRF from flagging, so row 1's CNVRF use is valid.  The health poll compares configuration bits only, and every DIAG_ALRT read feeds the classifier.
- DRV8316 read-back (:612): expected values match the written words.  The coasted value 0x90 is consistent with the per-drive coast paragraph (:686–688).
- Row 0 cross-check and plausibility (:652): the coasting-drum exclusions from round 20 are applied consistently.  The C1-only collapse (0.1 A × 100 ms / 374 µF ≈ 27 V) matches "~30 V".
- Drive-table preamble and drives resume (:673), rows 1–5 (:677–681), 6a (:683): internally consistent apart from B21-01, B21-04 and N2.  The §9 step 6 tests for resume, Release, shove, deaf/unpowered U4 and per-drive coast match the table.
- §9 steps 2 and 4–6: the step numbers cited from §8/§8.1 (step 4 idle current and t_split/t_RETRY, step 5 Z offset, step 6 row 5 threshold and ΔV/ΔI reference, resume brake bench check) all point to the step that performs them.

**Counts:** BLOCKER 0, MAJOR 2, MINOR 3, NOTE 3.
